# Types and data structures for crypto
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

struct CryptoConfig
    encoding
    iterations
    key_len
end

pub function default_crypto_config()
    return CryptoConfig {
        encoding: "hex",
        iterations: 10000,
        key_len: 32
    }
end
